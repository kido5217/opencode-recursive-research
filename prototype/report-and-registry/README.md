# PROTOTYPE — do not ship

Throwaway sample for Wayfinder ticket #20 (map #14): shows the shape of the closure
`report.md` and the `fixtures/registry.json` (+ schema) **before** the skill is rewritten.

**Illustrative content only.** The page text, URLs, hashes, and byte sizes are fabricated to
show structure; this is not a real capture. Throwaway branch: `prototype/report-and-registry`.

Layout mirrors the real run directory:

```
memory/research/caffeine-and-sleep-quality/
├── report.md
└── fixtures/
    ├── registry.schema.json
    ├── registry.json
    ├── webpage/webpage-a1b2c3d4e5f6/{page.md,raw.html}
    └── searchset/searchset-0f1e2d3c4b5a/results.json
```
